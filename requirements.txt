# Core (required)
requests>=2.28.0

# Drag & drop support (highly recommended on Windows)
tkinterdnd2>=0.3.0

# Optional - more file format support
python-docx>=0.8.11   # read/write .docx files
openpyxl>=3.1.0       # read/write .xlsx files
pandas>=1.5.0         # advanced data analysis
chardet>=5.0.0        # auto-detect file encoding (recommended)
